{
  "name": "portfolio-static",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist --project-name=portfolio-static --force"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "wrangler": "^4.111.0"
  }
}
